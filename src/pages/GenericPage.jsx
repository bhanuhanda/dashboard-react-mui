import { Typography } from '@mui/material';

// eslint-disable-next-line react/prop-types
export default function GenericPage({ pageName }) {
  return <Typography variant="h1">Welcome to {pageName} page!</Typography>;
}
